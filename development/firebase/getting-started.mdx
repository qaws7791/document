# firebase initialization

```typescript
npm install firebase
```


```typescript
// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAuth } from "firebase/auth";
import { getFirestore } from "firebase/firestore";
import { getStorage } from "firebase/storage";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyAf0ZhVcr-lQ0jhxqtffq-srktslGQ74s",
  authDomain: "youurproject.firebaseapp.com",
  projectId: "youurproject",
  storageBucket: "youurproject.appspot.com",
  messagingSenderId: "722415742807",
  appId: "1:722411343863:web:58fc3d75743988b929a68a",
  measurementId: "G-G7NWM81TP4",
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);

export const auth = getAuth(app);
export const db = getFirestore(app);
export const storage = getStorage(app);

export default app;

```

